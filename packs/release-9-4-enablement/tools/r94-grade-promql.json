{
  "id": "r94-grade-promql-tool",
  "type": "workflow",
  "configuration": {
    "workflow_id": "r94-grade-promql"
  },
  "description": "Grade a PromQL-in-ES|QL query. mode='canonical' verifies the seeded scenario produced runnable rows. mode='challenge' takes a user-supplied query and asserts the output matches a small spec (<=2 rows, has aggregation column). Returns {passed, hints[]}.",
  "tags": [
    "release-9-4-enablement"
  ]
}

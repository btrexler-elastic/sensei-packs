---
description: Spike skill testing whether allowed-tools accepts a YAML list. Use only if explicitly asked.
allowed-tools:
  - sensei-spike-analyze-tool
  - sensei-spike-install-plugin-tool
---

# Spike — allowed-tools (YAML list)

Confirms whether the install resolves both tools when allowed-tools is a YAML list.

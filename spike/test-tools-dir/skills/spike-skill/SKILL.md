---
description: Spike skill paired with a /tools directory in the plugin to test bundled tool definitions. Use only if the user explicitly asks the skill to run.
allowed-tools: spike-bundled-echo-tool
---

# Spike — tools-directory test

When invoked, call `spike-bundled-echo-tool` (which is supposed to ship inside this plugin's `tools/` directory) and report what happened.

---
description: Spike skill testing whether Elastic-API `tool_ids` frontmatter wires the skill to a cluster tool. Use only if the user explicitly asks the skill to run.
tool_ids:
  - sensei-spike-analyze-tool
---

# Spike — tool_ids test

When invoked, call `sensei-spike-analyze-tool` with `analyzer="standard"` and `text="hello world"`, then report whether the call succeeded.
